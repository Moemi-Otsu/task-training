<h1>タスク一覧</h1>

<table>
<%= @tasks.each do |task| %>
  <tr>
    <td><%= task.title %></td>
    <td><%= task.content %></td>
  </tr>
<% end %>
</table>

<div class="btns">
  <p><%= link_to '新規作成', new_task_path %></p>
  <p><%= link_to '一覧へ戻る', tasks_path %></p>
</div>